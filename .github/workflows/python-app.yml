# Acknowledgment:
# This CI configuration file was partially assisted by ChatGPT from OpenAI.
# ChatGPT helped in setting up the GitHub Actions workflow and configuring Python-based testing.

name: Python application

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Set up Python
        uses: actions/setup-python@v2
        with:
          python-version: '3.11.4'

      - name: Install Dependencies
      run: |
        pip install -r requirements.txt  # If you have a requirements file
        pip install pytest  # Ensure pytest is installed

    - name: Run Tests
      run: pytest

